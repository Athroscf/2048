<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.fiall.a2048">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:screenOrientation="portrait"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning,RtlEnabled,UnusedAttribute"
            tools:replace="android:allowBackup,icon,theme,label,name">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".ThreeTimesThree">
        </activity>
        <activity android:name=".FourTimesFour">
        </activity>
        <activity android:name=".SixTimesSix">
        </activity>
        <activity android:name=".EightTimesEight">
        </activity>
        <activity android:name=".Settings">
        </activity>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>

        <activity android:name=".HighScores">
        </activity>
    </application>

</manifest>